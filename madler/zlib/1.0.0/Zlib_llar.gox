// 声明相关信息

// 当前配方仅兼容1.0.0后的版本
fromVersion "1.0.0"

// 当前Package Name
packageName "madler/zlib"

// Package 描述
desc "A massively spiffy yet delicately unobtrusive compression library"

// Package 主页
homepage "https://github.com/madler/zlib"


onBuild => {
	echo "hello, zlib 1.0.0 \n Former ${lastArtifact}"
    return {Link: (args) => {
		return append(args, "-IZlib")
	}}, nil
}

onSource ver => {
	return "", nil
}

onVersions => {
	return nil
}
